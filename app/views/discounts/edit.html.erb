<%= render partial: "shared/nav" %>

<%= render partial: 'form', locals: { method: :patch, url: merchant_discount_path(@merchant, @discount), class: 'merchant_discounts_form', local: true, model: @discount } %>